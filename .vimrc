" Enable filetype plugins
filetype plugin on
filetype indent on

" Use spaces instead of tabs
set expandtab
" Be smart when using tabs ;)
set smarttab

" 1 tab == 4 spaces
set shiftwidth=4
set tabstop=4

" Show line numbers
set number

" Allow mouse usage
set mouse=a
